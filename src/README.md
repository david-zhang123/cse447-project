To run:

Execute (in a conda environment with all requirements)

Train run:

bash test.sh

To generate predictions run: (predict.sh should also work, but requires environment specification for paths for test data and test output)

bash test.sh

To evaluate predictions use:

python eval_test.py